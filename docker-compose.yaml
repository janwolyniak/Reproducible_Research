services:
  reproduction:
    build:
      context: .
      dockerfile: Dockerfile
    image: janwolyniak/reproducible-research-lic-fii:phase6
    command: python scripts/run_all.py
    environment:
      MPLBACKEND: Agg
    volumes:
      - .:/app
